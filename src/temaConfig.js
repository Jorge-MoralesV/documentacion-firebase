import { createTheme } from "@mui/material"

const theme = createTheme({
    palette: {
        primary: {
            main: '#2C2C2C'
        },
        secondary: {
            main: '#444444'
        }
    }
})

export default theme